Generar números enteros aleatorios dentro de un rango
Puedes generar un número entero aleatorio en el rango de cero a un número dado.
 También puedes elegir un número inferior diferente para este rango.

Llamarás a tu número mínimo miny a tu número máximo max.

Esta fórmula da como resultado un número entero aleatorio en el rango de 0 min a 0. max
Tómate un momento para leerla e intentar comprender qué hace este código:

Math.floor(Math.random() * (max - min + 1)) + min


Cree una función llamada randomRange que tome un rango myMiny myMax devuelva un número entero aleatorio 
que sea mayor o igual a myMiny menor o igual a myMax. 

function randomRange(myMin, myMax) {
  return 0;
}